#include "main.h"
#include <stdio.h>

/**
 * _strlen - func that returns d lenght of a strng.
 * @s: Type char pointer
 * Return: 0
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
		return (c);

}
