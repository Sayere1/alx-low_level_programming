#include <stdio.h>
/**
 * main - write a line with the print function not the puts function
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
